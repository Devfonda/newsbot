# NewsBot (clean starter)

1. copy .env.example -> .env and set BOT_TOKEN, CHANNEL_ID
2. pip install -r requirements.txt
3. python -m bot.bot_main    # run locally
4. use /test and /status commands in Telegram
5. For deployment: build docker or push to Railway/Heroku
